# This is a sample build configuration for C++.
# Check our guides at https://confluence.atlassian.com/x/VYk8Lw for more examples.
# Only use spaces to indent your .yml configuration.
# -----
# You can specify a custom docker image from Docker Hub as your build environment.
# image: quay.io/fenicsproject/dev-env-trilinos:latest
image: chrisrichardson/pyrol-testing:latest

pipelines:
  default:
    - step:
        script: # Modify the commands below to build your repository.
          - cd src # FIXME - put all this into a Makefile target
          - git clone https://github.com/pybind/pybind11.git
          - export TRILINOS_DIR=/usr/local
          - python setup.py install
          - source /home/fenics/fenics.env.conf
          - cd ..
          - py.test -s tests
            #           - . /home/fenics/firedrake/bin/activate
            #           - cd examples/firedrake_motherproblem/
            #           - python firedrake_motherproblem.py
